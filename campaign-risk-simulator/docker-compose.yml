services:
  consumer-research-framework:
    build: .
    container_name: consumer-research-framework
    restart: unless-stopped
    ports:
      - "6791:6791"
    env_file:
      - .env
    networks:
      - backend

networks:
  backend:
    external: true
    name: dbs_backend
